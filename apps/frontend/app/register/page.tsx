import AuthForm from "@/components/AuthForm";

export const metadata = { title: "Create Account | Closet by Chili" };

export default function RegisterPage() {
  return <AuthForm mode="register" />;
}
