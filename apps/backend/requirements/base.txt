# ================================================
# Closet by Chilli — Base Python Dependencies
# ================================================
# Shared across all environments.
# Pin major versions; lock exact versions via pip freeze.
# ================================================

Django>=5.1,<5.2
djangorestframework>=3.15,<4.0
django-cors-headers>=4.6,<5.0
psycopg2-binary>=2.9,<3.0
celery>=5.4,<6.0
redis>=5.2,<6.0
gunicorn>=23.0,<24.0
